{
  "name": "autonomous-research-agent-frontend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --configLoader native",
    "dev:lan": "vite --configLoader native --host 0.0.0.0",
    "build": "vite build --configLoader native",
    "preview": "vite preview --configLoader native",
    "preview:lan": "vite preview --configLoader native --host 0.0.0.0"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "vite": "latest",
    "react": "latest",
    "react-dom": "latest",
    "axios": "latest"
  },
  "devDependencies": {}
}
